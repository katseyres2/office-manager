<Window x:Class="WPF.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:WPF"
    mc:Ignorable="d"
    Title="Flexi Workspace" Height="450" Width="800">
    <TabControl>
        <TabItem Header="Owners">
            <DataGrid Name="dataGridOwners" CanUserSortColumns="False" AutoGenerateColumns="False" ItemsSource="{Binding Owners}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding OwnerId}" IsReadOnly="True" Visibility="Visible" SortDirection="Ascending"/>
                    <DataGridTextColumn Header="Label" Binding="{Binding Label}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="VAT" Binding="{Binding Tva}" IsReadOnly="True"/>
                    <DataGridCheckBoxColumn Header="Active" Binding="{Binding Active}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="CreatedAt" Binding="{Binding CreatedAt}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="UpdatedAt" Binding="{Binding UpdatedAt}" IsReadOnly="True"/>
                </DataGrid.Columns>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <EventSetter Event="PreviewMouseDoubleClick" Handler="DataGridCell_PreviewMouseDoubleClick"/>
                    </Style>
                </DataGrid.CellStyle>
            </DataGrid>
        </TabItem>
    </TabControl>
</Window>
